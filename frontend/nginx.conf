server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api-v1.0/ {
        proxy_pass http://backend:9080/api-v1.0/;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}